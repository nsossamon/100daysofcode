name = "Jack"
print(name)

# previous problem reiterated with variables
name = input('What is your name ')
length = len(name)
print(length)

# separate multiple words in a variable with "_" , can't use a space
user_name = input('What is your name? ')

# can use numbers, but only at end of the name (e.g. user1, but not 1user)
# also bad practice to use function names as variables (e.g. input = input('what is your name?')

time_until_midnight = '5'
print('There are ' + time_until_midnight + ' hours until midnight')
